on:
    push:
        branches:
            - test-branch
name: release-please
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: GoogleCloudPlatform/release-please-action@v2
        with:
          release-type: node
          package-name: release-please-action
          path: $GITHUW_WORKSPACE/path1/
